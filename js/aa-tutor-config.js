/* Amp Academy Tutor config — EDIT THIS ONE FILE after you deploy the relay worker.
 * Replace <acct> with your real Cloudflare Worker URL. Until you do, the endpoint is a
 * placeholder and the tutor stays OFFLINE (no behavior change). One file, all pages. */
window.AA_TUTOR = {
  endpoint: "https://amp-academy-tutor.ampacademy.workers.dev/tutor",
  edition: "your edition of the NEC"
};
